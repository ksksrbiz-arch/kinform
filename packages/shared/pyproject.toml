[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "kinform-shared"
version = "1.0.0"
description = "Shared governance rules and validators for KINFORM-AEO."
requires-python = ">=3.11"
authors = [{ name = "1Commerce — KINFORM" }]
license = { text = "Proprietary" }
dependencies = []

[tool.setuptools.packages.find]
where = ["python"]
