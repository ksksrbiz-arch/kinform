{
  "name": "@kinform/shared",
  "version": "0.1.0",
  "private": true,
  "description": "KINFORM shared constants and governance rules — single source of truth for brand tokens, content rules, and validation primitives used by the storefront, the Payload Studio IDE, and the PersonaGenAI service.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
